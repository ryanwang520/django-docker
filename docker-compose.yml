version: "3"

services:
  web:
    build: .
    image: moonshadow/django-docker:${IMAGE_TAG:-latest}
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      DEV_MODE: 1
